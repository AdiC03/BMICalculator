import 'package:flutter/material.dart';

// k is naming convention for constants (flutter knows this)

const kLabelTextStyle = TextStyle(
    fontSize: 18,
    color: Color(0xffcfd8dc),
);

const kDataLabelStyle = TextStyle(
        fontSize: 50,
        fontWeight: FontWeight.w700,
);

const kResultStyleText = TextStyle(
  fontSize: 22,
  color: Color(0xFF101981),
  fontWeight: FontWeight.bold,
);

const kBMITextStyle = TextStyle(
  fontSize: 80,
  fontWeight: FontWeight.bold,
);

// Main use of const
const double kBottomContainerHeight = 80;
const kRegCardColor = Color(0xff283593);
const kPressedCardColor = Color(0xff5c6bc0);